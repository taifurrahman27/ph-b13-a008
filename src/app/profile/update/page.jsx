import UpdateProfilePage from '@/components/UpdateProfilePage';
import React from 'react';

const page = () => {
    return (
        <div>
            <UpdateProfilePage />
        </div>
    );
};

export default page;